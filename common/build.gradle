plugins {
    id 'java'
}

group 'com.epam.mentoring.program'

repositories {
    mavenCentral()
}

//project.description = "UI tests for report portal"
//project.archivesBaseName = "$applicationName" + '-common'
//project.version = "$applicationVersion"